// XXX: we can maybe get away without usng Vuex for now.

import Vue from 'vue';
import Vuex from 'vuex';

import io from 'socket.io-client';

Vue.use(Vuex);

const actions = {

}

const state = {
}

const mutations = {
}

export default new Vuex.Store({
	state, mutations
});
